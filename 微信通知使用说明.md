# 微信通知功能使用说明

## 快速开始

### 1. 安装和配置
```bash
# 运行安装向导
python setup_wechat.py
```

安装向导会自动：
- 检查Python版本
- 安装必要依赖
- 检查微信应用
- 引导权限设置
- 配置通知参数

### 2. 手动配置（可选）

如果需要手动配置，编辑 `config.json` 文件：

```json
{
  "notification": {
    "wechat": {
      "enabled": true,
      "method": "applescript",
      "groups": ["订单通知群", "运营群"],
      "template": {
        "new_order": "🎉 新订单通知 🎉\n\n订单信息：\n- 主播：{anchor_name}\n- 商品：{product_name}\n- 数量：{quantity}\n- 金额：¥{amount}\n- 时间：{order_time}\n\n请及时处理！"
      }
    }
  }
}
```

### 3. 测试功能
```bash
# 运行测试脚本
python test_wechat_notification.py
```

## 权限设置

### macOS 辅助功能权限
1. 打开 `系统偏好设置` > `安全性与隐私` > `隐私`
2. 选择 `辅助功能`
3. 点击锁图标解锁
4. 添加以下应用之一：
   - Python解释器 (通常在 `/usr/bin/python3` 或虚拟环境中)
   - 终端应用 (`/Applications/Utilities/Terminal.app`)
   - 你的IDE (如VSCode、PyCharm等)
5. 确保勾选启用

### 屏幕录制权限（仅PyAutoGUI方案需要）
如果使用PyAutoGUI方案，还需要在同一个隐私设置页面：
1. 选择 `屏幕录制`
2. 添加相同的应用
3. 确保勾选启用

## 使用方式

### 自动通知
微信通知已集成到现有的订单处理流程中，当有新订单同步到飞书时会自动发送通知。

### 手动发送
```python
from notification_sender import NotificationSender

# 创建通知发送器
sender = NotificationSender()

# 准备订单数据
order_data = {
    'anchor_name': '张三',
    'product_name': '测试商品',
    'quantity': 1,
    'amount': '99.00',
    'order_time': '2024-03-10 15:30:00'
}

# 发送通知
success = sender.send_order_notification(order_data)
```

## 配置说明

### 基本配置
- `enabled`: 是否启用微信通知 (true/false)
- `method`: 发送方式 ("applescript" 推荐 或 "pyautogui")
- `groups`: 目标群聊名称列表

### 消息模板
可以自定义通知消息模板，支持以下变量：
- `{anchor_name}`: 主播名称
- `{product_name}`: 商品名称
- `{quantity}`: 数量
- `{amount}`: 金额
- `{order_time}`: 订单时间
- `{current_time}`: 当前时间

## 故障排除

### 常见问题

1. **权限不足**
   - 确保已授予辅助功能权限
   - 重启应用或重新授权

2. **微信未激活**
   - 确保微信窗口可见且未最小化
   - 检查微信是否已登录

3. **群聊名称错误**
   - 检查群聊名称是否完全匹配（区分大小写）
   - 确认群聊在微信中可见

4. **发送失败**
   - 检查网络连接
   - 确认微信客户端正常工作
   - 查看日志文件了解详细错误

### 调试模式
启用详细日志：
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 测试步骤
1. 运行 `python test_wechat_notification.py`
2. 检查控制台输出
3. 确认微信群中收到测试消息

## 安全建议

1. **发送频率控制**
   - 避免短时间内大量发送消息
   - 建议设置发送间隔

2. **权限管理**
   - 只授予必要的系统权限
   - 定期检查权限设置

3. **配置安全**
   - 不要在配置文件中存储敏感信息
   - 定期备份配置文件

4. **更新维护**
   - 定期检查微信客户端更新
   - 及时调整脚本适配界面变化

## 技术原理

### AppleScript方案 (推荐)
- 使用macOS原生AppleScript控制微信
- 通过系统事件模拟键盘操作
- 兼容性好，不易被检测

### PyAutoGUI方案
- 通过屏幕截图和图像识别定位界面元素
- 模拟鼠标和键盘操作
- 需要预先截取模板图片

## 支持

如有问题，请检查：
1. 日志文件 (`logs/` 目录)
2. 配置文件是否正确
3. 权限设置是否完整
4. 微信客户端是否正常