# 环境切换说明

## 配置文件说明

- `config.prod.json` - 正式环境配置
- `config.test.json` - 测试环境配置  
- `config.json` - 当前使用的配置（由切换脚本自动生成）

## 环境信息

### 正式环境
- **App ID**: `cli_a9200e9641fadbc0`
- **App Secret**: `9tjBqUG6qRtDWxNqybbHeeNdx1A1hoSi`
- **Wiki URL**: https://my.feishu.cn/wiki/PWMUwFVPni4WY9kRunEckmW9n0g
- **域名**: `my.feishu.cn`

### 测试环境
- **App ID**: `cli_a92cd34869b9dbd6`
- **App Secret**: `vYcKIRk6icY1f8xXlig7NcY0gIWxQY6t`
- **Wiki URL**: https://vcn13vbsobtc.feishu.cn/wiki/WuLowQPlOigCT9kQ089chCfun2e
- **域名**: `vcn13vbsobtc.feishu.cn`

## 使用方法

### 方法 1: 使用切换脚本（推荐）

```bash
# 切换到正式环境
./switch_env.sh prod

# 切换到测试环境
./switch_env.sh test

# 查看当前环境
./switch_env.sh
```

### 方法 2: 手动复制配置文件

```bash
# 切换到正式环境
cp config.prod.json config.json

# 切换到测试环境
cp config.test.json config.json
```

## 运行程序

切换环境后，直接运行程序即可：

```bash
# Web 服务器模式
./.venv/bin/python web_control_server.py --config config.json --port 8010

# 单次同步
./.venv/bin/python sync_to_feishu.py --config config.json
```

## 注意事项

⚠️ **重要**：
1. 切换环境后，程序会使用对应环境的应用凭证和云文档
2. 测试环境和正式环境的数据是**完全隔离**的
3. 修改配置时，请同时更新 `config.prod.json` 或 `config.test.json`
4. **不要直接修改 `config.json`**，因为切换环境时会被覆盖

## 验证环境

切换环境后，可以运行测试脚本验证：

```bash
./.venv/bin/python test_api_access.py
```

成功输出示例：
```
✅ 令牌获取成功
✅ 找到 5 个工作表
✅ 找到 XXX 个订单号
```
